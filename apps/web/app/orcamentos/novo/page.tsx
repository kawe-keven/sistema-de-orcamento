'use client';
import { useState } from 'react';
export default function NewQuotePage() { const [step, setStep] = useState(1); return <main className="min-h-screen p-8 md:p-12"><div className="mx-auto grid max-w-6xl gap-6 lg:grid-cols-2"><section><p className="text-xs font-bold uppercase tracking-[.2em] text-[#2f8d61]">workspace / novo orçamento</p><h1 className="mt-2 text-4xl font-bold">Vamos montar uma proposta.</h1><div className="card mt-8 p-6"><div className="mb-8 flex gap-3 text-sm font-bold text-[#738077]">{['Cliente','Projeto','Revisar'].map((label,index) => <span className={step === index + 1 ? 'text-[#2f8d61]' : ''} key={label}>{index + 1}. {label}</span>)}</div>{step === 1 && <div><label className="mb-2 block text-sm font-bold">Nome do cliente<input className="mt-2 w-full rounded-md border border-[#e5eae3] p-3" placeholder="Ex.: Estúdio Aurora" /></label><label className="mt-5 block text-sm font-bold">E-mail<input className="mt-2 w-full rounded-md border border-[#e5eae3] p-3" type="email" placeholder="cliente@email.com" /></label></div>}{step === 2 && <div><label className="mb-2 block text-sm font-bold">Projeto<input className="mt-2 w-full rounded-md border border-[#e5eae3] p-3" placeholder="Identidade visual" /></label><label className="mt-5 block text-sm font-bold">Descrição<textarea className="mt-2 min-h-28 w-full rounded-md border border-[#e5eae3] p-3" /></label></div>}{step === 3 && <p className="text-[#738077]">Revise os dados e envie quando estiver pronto.</p>}<div className="mt-8 flex justify-between"><button className="rounded-md border border-[#e5eae3] px-4 py-3" onClick={() => setStep(Math.max(1, step - 1))}>Voltar</button><button className="rounded-md bg-[#17221d] px-4 py-3 font-bold text-white" onClick={() => setStep(Math.min(3, step + 1))}>{step === 3 ? 'Enviar orçamento' : 'Continuar →'}</button></div></div></section><aside className="card h-fit p-8"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#2f8d61]">Preview do cliente</p><h2 className="mt-8 text-2xl font-bold">&gt;_ workspace</h2><p className="mt-5 text-[#738077]">O cliente verá a proposta sendo atualizada em tempo real.</p></aside></div></main>; }
